export const sum = (nb1: number, nb2: number) => nb1 + nb2;
export const multipli = (nb1: number, nb2: number) => nb1 * nb2;

// fonction qui soustrait 2 nombres
export const sub = (nb1: number, nb2: number) => {
  return nb1 - nb2;
};
